//tipagem dinâmica: as variáveis podem receber valores de tipos diferentes

//objetos (tipos por referência)
let pessoa = {
    especie: humano,
     nome,
     idade,
     sexo: 'M',
     peso: 78.5,
     estaAprovado,
     sobrenome: 'Silva'
};
console.log(pessoa)